{
  "name": "me-api",
  "version": "3.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "nodemon app.js",
    "watch": "npm run stop && nodemon app.js",
    "stop": "npx kill-port 1337",
    "production": "NODE_ENV='production' nodemon app.js",
    "test": "nyc --reporter=html --reporter=text --reporter=clover mocha --timeout 10000 ./test/**/*.js --exit",
    "test-scrutinizer": "nyc --all --reporter=clover mocha test/**/*.js",
    "report-coveralls": "nyc report --reporter=text-lcov | coveralls",
    "report-codecov": "nyc report --reporter=lcov > coverage.lcov && codecov",
    "clean": "rm -rf node_modules package-lock.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "codecov": "^3.8.3",
    "cors": "^2.8.5",
    "coveralls": "^3.1.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-validator": "^6.2.0",
    "mongoose": "^5.7.8",
    "morgan": "^1.10.0",
    "nodemon": "^1.19.4",
    "socket.io": "^4.2.0",
    "uuid": "^3.3.3"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "chai-http": "^4.3.0",
    "mocha": "^9.1.1",
    "nodemon": "^1.19.4",
    "nyc": "^15.1.0"
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.3",
    "utf-8-validate": "^5.0.5"
  }
}
