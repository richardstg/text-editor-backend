# Text-Editor API

## Commands
Install modules by executing "npm install".
Start the server by executing "npm start" or "npm run watch".

## Routes
Get all texts, GET request to "/".
Get a text by text id, GET request to "/:id".
Add a text, POST request to "/".
Update a text, PUT request to "/:id".
